function [err] = common_err(code, msg, varargin)
    err.code = code;
    err.msg = msg;
    err.context = [];
    if nargin > 2
        for i = 3:nargin
            err.context(i-2) = varargin{i-2};
        end        
    end
end